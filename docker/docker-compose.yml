version: '3'
services:
  twks-server:
    build:
      context: ..
      dockerfile: docker/server/Dockerfile
    container_name: "twks-server"
    image: tetherlessworld/twks-server
    ports:
      - "8080:8080"
    volumes:
      - twks-data:/data

volumes:
  twks-data:
